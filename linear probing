//hasing using division and linear probing meethod

#include<stdio.h>
int main()
{
    int arr[20],key,hash,size,i,j=0;
    printf("\nenter the size of the array\n");
    scanf("%d",&size);
    for(i=0;i<size;i++)
    {
        arr[i]=-1;
    }
    while(1)
    {
    if(j==size)
            break;
    else
    {
        printf("\nenter the key:\n");
        scanf("%d",&key);
        hash=key%size;
        
        if(arr[hash]==-1)
        {
            arr[hash]=key;
            j++;
            for(i=0;i<size;i++)
            {
                printf("%d\n",arr[i]);
            }
        }
        else
        {
            printf("collusion occurs at %d",hash);
            for(i=hash+1;i<size;i++)
            {
               if(arr[i]==-1)
               {
                   arr[i]=key;
                   j++;
                   for(i=0;i<size;i++)
                   {
                       printf("%d\n",arr[i]);
                   }
                   break;
               }
               else if(i==size-1)
               {
                   i=-1;
               }
            }
        }
    }
}
}
